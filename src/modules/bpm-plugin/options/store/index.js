const state = {
    asiderMenu: []
};

const mutations = {
    SET_ASIDER_MENU(state, val) {
        state.asiderMenu = val;
    }
};

const actions = {
};

const getters = {

};

export default {
    state,
    mutations,
    actions,
    getters
};
